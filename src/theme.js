import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#ff9800',
      light: '#0288d1',
    },
    secondary: {
      main: '#1b5e20',
    },
    otherColor: {
      main: '#999',
    },
  },
});
